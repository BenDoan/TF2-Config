bind w +forward				
bind s +back			
bind a +moveleft			
bind d +moveright	
bind mouse1 +attack
bind mouse2 +attack2
bind h +quickswitch
bind k explode
bind q +duck
bind f changeclass
bind mouse4 +voicerecord
bind f1 screenie
bind n "sm_nominate"
bind c +smiley_dice
bind tab +score
bind f2 fix
bind x radiocyc
bind r "voicemenu 0 0"
bind space +jumpeh
bind f3 prec_record
bind shift prec_mark
bind f6 person1
bind g taunt
bind t "impulse 201"
bind enter say


bind kp_5 wswitch4
bind kp_rightarrow nomnom
bind KP_end "say Type record demo and then stop in console."
bind kp_uparrow sm_admin
bind kp_downarrow "say !stop"
bind kp_pgdn "say Type !stop in chat to stop the music."
bind kp_leftarrow restartgame
bind kp_leftarrow "sm_csay Spawn camping is against server rules."

unbind mouse5
unbind ctrl

echo =================================================================================
echo binds executed
echo =================================================================================